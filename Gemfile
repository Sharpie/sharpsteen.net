source 'https://rubygems.org'

gem 'jekyll'
# We have a couple custom plugins, so we're locking the version Just In Case™.
gem 'redcarpet', '~> 3.1.2'

# Custom additions go here.
eval_gemfile "#{__FILE__}.local" if File.exists? "#{__FILE__}.local"
